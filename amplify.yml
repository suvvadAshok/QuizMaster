version: 1
frontend:
  phases:
    preBuild:
      commands:
        - rm -rf Client/node_modules
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /Client/dist
    files:
      - "**/*"
  cache:
    paths:
      - Client/node_modules/**/*
